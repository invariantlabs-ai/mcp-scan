# {{ REQUIRES: []}}

raise PolicyViolation("Detected link in tool output.") if:
    (tooloutput: ToolOutput)
    {{ BLACKLIST_WHITELIST }}
    any(["https://" in t for t in  text(tooloutput.content)])
 
raise PolicyViolation("Do not talk about Fight Club") if:
    (msg: Message)
    msg.role == "user"
    "fight club" in msg.content
 
 